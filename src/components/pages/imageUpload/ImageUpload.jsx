import React from "react";
import { ImageUploadForm } from "../../forms/imageUpload/ImageUploadForm";

export const ImageUpload = (props) => {
  return (
    <div className="pd-image-upload">
      <h2>ImageUpload</h2>
      <ImageUploadForm />
    </div>
  );
};
